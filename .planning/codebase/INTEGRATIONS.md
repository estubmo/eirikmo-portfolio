# External Integrations

**Analysis Date:** 2026-06-01

## APIs & External Services

**GitHub REST API:**
- Fetches live repo stats (stars, forks, watchers, last updated) for display in the site footer
  - SDK/Client: Nuxt `useFetch` (no SDK; plain REST call)
  - Endpoint: `https://api.github.com/repos/estubmo/eirikmo-portfolio`
  - Auth: None (public endpoint; no token required)
  - Location: `src/components/FooterComponent.vue` line 10

**Google Fonts CDN:**
- Loads Inter typeface (weights 200–800) at build/request time
  - Module: `@nuxtjs/google-fonts` 3.2.0
  - Config: `nuxt.config.ts` — `display: "swap"`, family `Inter: "200..800"`
  - No API key required

## Data Storage

**Databases:**
- None - portfolio is fully static content; no database connection

**File Storage:**
- Local filesystem only - static assets served from `src/public/`
  - 3D models: `src/public/models/*.glb` (desktop, keyboard, lamp, mobile, mouse, tablet)
  - Textures: `src/public/textures/` (per-project/work screenshot textures loaded by Three.js `TextureLoader`)
  - Images: `src/public/images/` (project screenshots, work logos, profile photo)

**Caching:**
- Nuxt ISR (Incremental Static Regeneration) - all routes revalidate every 3600s
  - Config: `nuxt.config.ts` `routeRules: { "/**": { isr: 60 * 60 } }`

## Authentication & Identity

**Auth Provider:**
- None - no user authentication required; public portfolio site

## SEO & Discovery

**Sitemap:**
- Auto-generated by `@nuxtjs/sitemap` 7.6.0
  - Output URL: `https://mowebdev.com/sitemap.xml`
  - Referenced in `robots.txt` via `@nuxtjs/robots`

**Robots:**
- `@nuxtjs/robots` 5.7.0
  - Config: `nuxt.config.ts` — `UserAgent: "*"`, `Disallow: ""` (all crawlers allowed)

**Schema.org / Structured Data:**
- `nuxt-schema-org` 5.0.10
  - Site name: `"Eirik Mo - Senior Full-Stack Developer"`
  - Site URL: `https://mowebdev.com`

## Monitoring & Observability

**Error Tracking:**
- Not detected

**Performance:**
- `CustomStatsGl.vue` - dev-only WebGL stats overlay (stats-gl library, used inside TresJS canvas)

**Logs:**
- No structured logging detected; browser console only

## CI/CD & Deployment

**Hosting:**
- Vercel (referenced explicitly in `src/components/FooterComponent.vue` and `nuxt.config.ts` site URL)
- No `vercel.json` present; deployment relies on Vercel's automatic Nuxt detection

**CI Pipeline:**
- None detected - no `.github/workflows/`, no CI config files present

## Environment Configuration

**Required env vars:**
- None detected - no external service credentials needed; the only external call (GitHub API) is unauthenticated

**Secrets location:**
- No `.env` file present; no secrets in use

## Webhooks & Callbacks

**Incoming:**
- None

**Outgoing:**
- None

## Social Profile Links

The site links to these external social profiles (static hrefs, no API):
- GitHub: `https://github.com/estubmo/` - `src/components/SocialsComponent.vue`
- Instagram: `https://www.instagram.com/estubmo/` - `src/components/SocialsComponent.vue`
- X (Twitter): `https://x.com/eirikm0/` - `src/components/SocialsComponent.vue`
- LinkedIn: `https://www.linkedin.com/in/eiriksmo/` - `src/components/SocialsComponent.vue`
- CodeSandbox: `https://codesandbox.io/u/estubmo` - `src/components/SocialsComponent.vue`

---

*Integration audit: 2026-06-01*
